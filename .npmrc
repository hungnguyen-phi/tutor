auto-install-peers=true
strict-peer-dependencies=false
# Don't auto-reinstall before every script run (avoids unapproved-build exit code
# blocking typecheck/test). Builds are allowlisted in pnpm-workspace.yaml.
verify-deps-before-run=false
