/* Trang giới thiệu công khai (Landing cũ) — dời khỏi cửa chính "/" theo
   quyết định "mới vào app redirect về login hết". Login trỏ tới đây cho
   footnote PDPL "Tìm hiểu thêm". */

import Landing from "../../components/Landing";

export default function GioiThieuPage() {
  return <Landing />;
}
